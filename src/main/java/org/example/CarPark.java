package org.example;

import java.util.ArrayList;
import java.util.List;

/*
generate car park - takes 3 numbers representing how many of each space it has

 */

public class CarPark {
    private int totalCapacity;
    private List<Integer> parkingSpaces = new ArrayList<>();
    private List<Integer> parkedVehicles = new ArrayList<>();

    public void generateCarPark(int smallSpaces, int mediumSpaces, int largeSpaces) {
        while
    }

    public CarPark(int totalCapacity) {
        this.totalCapacity = totalCapacity;
    }

    public void addParkedVehicles(Integer vehicle) {
        parkedVehicles.add(vehicle);
    }

    public void addParkingSpaces(Integer space) {
        parkingSpaces.add(space);
    }
}
